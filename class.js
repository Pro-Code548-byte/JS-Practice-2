function printClassList() {
  let classlist = ["Amara", "Dele", "Chinwe", "Tunde", "Femi", "Nneka"];

  for (let i = 0; i < classlist.length; i++) {
    console.log("Student " + (i + 1) + ": " + classlist[i]);
  }
}

printClassList();
