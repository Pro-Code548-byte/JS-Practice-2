function updateCart() {
  let cart = ["shirt", "shoes", "hat"];

  // Add "belt" and "watch" to the cart
  cart.push("belt", "watch");

  // Print the total number of items
  console.log(cart.length);
}

updateCart();
